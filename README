XHProf List

How to use?
1. Drop the files into a directory that is accessible from say, URL1.
2. In the conf.php file set:
   XHPROF_DATA_DIR to the directory containing the XHProf output
   XHPROF_URL to the base URL that is used for viewing XHProf output
3. Access URL1, you should see something similar to screenshot.jpeg. 
